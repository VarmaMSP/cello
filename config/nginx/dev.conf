events {}

http {
    include mime.types;

    server {
        listen 8080;
        server_name localhost;

        
        location /img/ {
            root /var/www/;
        }

        location /static/ {
            root /var/www/;
        }

        location /api/ {
            proxy_pass http://localhost:8081/;
        }

        location / {
            proxy_pass http://localhost:8082/;
        }
    }
}