MINIO_ACCESS_KEY="{{ passwords.access_key }}"
MINIO_SECRET_KEY="{{ passwords.secret_key }}"
MINIO_VOLUMES="{{ minio_data_dir }}"
MINIO_OPTS="-C {{ minio_config_dir }} --address {{ hostvars[inventory_hostname]['private_ip'] }}:9000"
